<?xml version="1.0" encoding="UTF-8"?>
<configuration>

<!-- 
	로그 레벨 수준
	1. TRACE - 자세히 모두 출력. 특히 루프문의 내부 흐름을 제어할 때 예) for 문에서 3번째 i 가 현재 어떤 값인지 등(거의 안 씀)
	2. Debug - 개발단계에서 가장 많이 사용
					로직의 흐름 확인 용, 로직의 주요 분기, 샐행부가 메서드에 진입/종료 했는지 확인용
	3. INFO - 개발단계 보다는 운영 쪽에서 많이 사용
				   서버의 시작/종료 여부 등을 출력, 주요 이베트 발생을 알리기 위해
				   개발에서는 사용자가 로그인을 성공했는지, 주문을 완료했는지 등 큰 이벤트에서 사용
	4. WARN - 당장 장애는 아니지만, 곧 문제가 될 수 있는 소지가 있을 경우(신호등의 노란불 느낌)
	5. ERROR - 예외가 발생한 경우, 요청 처리 실패 시(빨간 불)
	6. FATAL  - 있어선 안될 오류. 서비스 못하는 수준.
-->

	<!-- 콘솔에 출력만 할 용도로 설정. 파일로 저장도 가능하다. -->
	<appender name = "CONSOLE" class = "ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<!-- 개발자가 원하는 패턴을 명시 -->
			<pattern>
				%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{40} - %msg%n
			</pattern>
		</encoder>
	</appender>

	<!-- 
	어느 수준으로 로그를 사용할 지 레벨을 등록. 개발자가 로그레벨을 선언하면, 로그는 그 선언된 레벨 이상의 로그만 출력
	-->
	<root level="INFO">
		<appender-ref ref = "CONSOLE"></appender-ref>
	</root>
	
	<!-- 
		아래처럼 패키지 별로 로그 레벨을 설정하면, 더 구체적일 수록 로그의 적용 우선순위가 높아지므로
		위에서 root 로그레벨을 선언하였다 할지라도, 아래의 DEBUG 로그가 우선적용된다
	 -->
	<logger name="com.ch.shop.controller" level="DEBUG"></logger>

</configuration>